const CDN_BASE_URL = 'https://cdn.example.com';

const ENABLE_MOCK = true;

const ENABLE_SEED_DATA = true;

const SEED_DATA_CONFIG = {
  lostFound: true,
  market: true,
  studyMaterials: true,
  studyRewards: true,
  survey: true,
  campusShop: true,
  volunteer: true,
  errand: true,
  rental: true,
  carpool: true,
  canteen: true,
  forum: true,
  club: true,
  scenery: true
};

const URL_CHECK_TIP = '请输入有效的URL地址';

module.exports = {
  CDN_BASE_URL,
  ENABLE_MOCK,
  ENABLE_SEED_DATA,
  SEED_DATA_CONFIG,
  URL_CHECK_TIP
};
